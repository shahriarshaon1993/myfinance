<script setup lang="ts">
import HeadingSmall from '@/components/HeadingSmall.vue';
import RoleForm from '@/components/app/role/RoleForm.vue';
import { Button } from '@/components/ui/button';
import AppLayout from '@/layouts/AppLayout.vue';
import { edit, index } from '@/routes/roles';
import { type BreadcrumbItem, ModuleWithPermissions } from '@/types';
import { Role } from '@/types/roles';
import { Head } from '@inertiajs/vue3';

defineProps<{ role: Role; }>();

const breadcrumbs: BreadcrumbItem[] = [
    { title: 'Roles', href: index().url },
    { title: 'Edit', href: edit(1).url },
];
</script>

<template>
    <Head title="Edit Role" />

    <AppLayout :breadcrumbs="breadcrumbs">
        <div
            class="flex h-full flex-1 flex-col gap-4 overflow-x-auto rounded-xl p-4"
        >
            <div class="space-y-6">
                <!-- Heading -->
                <HeadingSmall
                    title="Update Role"
                    description="Update a role and assign permissions."
                >
                    <Button
                        variant="link"
                        as="a"
                        :href="index().url"
                        class="cursor-pointer"
                    >
                        <span>Back to Roles</span>
                    </Button>
                </HeadingSmall>
                <!-- Form -->
                <RoleForm :role="role" />
            </div>
        </div>
    </AppLayout>
</template>
